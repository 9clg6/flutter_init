import 'package:flutter/painting.dart';

/// Custom colors that are not related to [ThemeAppearance].
///
/// Please if the color you want to create is related to it
/// then consider to add your color in [AppColors] then instanciate it
/// in the different appearances.
class CustomColors {
  /// Error color
  static const Color errorColor = Color(0xfff81554);
}
